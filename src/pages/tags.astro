---
import Layout from '../layouts/Layout.astro';

const API_URL = import.meta.env.API_URL;

const tags_data = await fetch (`${API_URL}/api/v2/tags`);
const tags = await tags_data.json();

const pageTitle = 'Tags';
---

<Layout pageTitle={pageTitle}>
    <main>
        <div class="mb-5">
            <h2 class="text-2xl">Tags</h2>
            <p>All the tags used.</p>
        </div>

        <div class="flex lg:flex-row flex-col">
            {
                tags.items?.map((item) => (
                    <a href="" class="text-xl me-5 mb-3 lg:mb-0 font-bold text-[#D81159] hover:border-b-4 hover:border-[#FF80CC] hover:border-dashed">
                    {`#${item.name}`}
                    </a>
                ))
                
            }
        </div>
    </main>

</Layout>